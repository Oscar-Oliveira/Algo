algoritmo "MeuPrograma"

estrutura Ponto
    x:inteiro
    y:inteiro

inicio
    escrever("ola")
    a:Ponto = {x: 1, y: 2}
    b:Ponto = a
    b.x = 99
    escrever("ola111")

    ler(b.y)

    escrever(a.x, " ", b.x)