# Matriz de rastreabilidade — Etapa 0

Produzida por leitura integral de `docs/ReferenciaCompletaCLI.md`,
`docs/ManualCompletoALGO.md`, `docs/RoteiroTestesManualALGO.md`, os 3
ficheiros `.docx` (texto extraído via `zipfile`/regex, já que não são
lidos diretamente), e cruzamento com os nomes de teste reais em
`algo_lang/tests/` (grep, não execução completa nesta etapa). Nenhum
código foi alterado.

Formato: `Requisito | Componente | Testes existentes (amostra) | Estado
Etapa 0 | Nota`. "Estado Etapa 0" é uma primeira leitura por nomes de
teste — a confirmação real (correr os testes, ler o corpo) acontece na
etapa dedicada a cada área, não aqui.

## 1. Léxico

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| 33 palavras-chave reconhecidas | `lexer.py` | indireto (quase todo teste do compilador) | coberto indiretamente | sem teste que enumere as 33 uma a uma; a Etapa 1 deve construir essa lista e confirmar cobertura individual |
| Comentários linha/bloco | `lexer.py` | `test_novas_funcionalidades.py::test_comentario_*` (5), `test_correcoes_auditoria.py::test_comentario_de_bloco_*` (2) | bem coberto | |
| Indentação (tabs/espaços/saltos) | `lexer.py` | `test_correcoes_auditoria.py::test_lexer_mistura_tabs_espacos`, `::test_lexer_espacos_nao_multiplo_de_4`, `::test_lexer_indentacao_avanca_mais_de_um_nivel_de_uma_vez`, `::test_lexer_tab_a_meio_de_linha_e_tratado_como_espaco` | bem coberto | história de 2 rondas de bugs nesta área (AL-15, B1/B2 de duas auditorias) — mesmo assim vale reconfirmar na Etapa 1 |
| Literais (5 tipos) | `lexer.py` | espalhados por quase toda a suite | coberto | notação científica confirmada **não suportada** (decisão documentada, `AL-14`) |
| **Posição linha/coluna em erros** | `lexer.py:39-45`, `parser.py:25-30` | `test_correcoes_auditoria.py::test_erro_sintatico_inclui_coluna_correta`, `::test_erro_lexico_inclui_coluna_correta`, `::test_coluna_e_relativa_a_linha_original_incluindo_indentacao_profunda`, `::test_erro_de_indentacao_sem_coluna_especifica_continua_a_funcionar` | **corrige suposição da 3ª auditoria** | `AUDITORIA.md` (3ª auditoria) listava "falta de coluna" como melhoria nunca implementada — **falso hoje**: `ErroLexico` e `ErroSintatico` já reportam coluna, com 4 testes dedicados. **Mas `ErroSemantico` (`semantics.py:14-18`) não tem parâmetro `coluna` nenhum** — assimetria real, a confirmar como finding na Etapa 3, não na Etapa 1. |
| Unicode/acentuação (identificadores, strings) | `lexer.py` | não localizado teste explícito | **lacuna confirmada** | manter na Etapa 1 |

## 2. Sintático

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Um único bloco `inicio` | `parser.py` | `test_correcoes_auditoria.py::test_segundo_bloco_inicio_e_rejeitado` (comentário `B4`/`AL-75`) | **corrige suposição do plano** — já coberto | o plano (Fase 2) tinha isto como exemplo de risco histórico; confirmado corrigido e testado |
| Limite de profundidade de expressão/bloco | `parser.py` | `test_correcoes_auditoria.py::test_expressao_fortemente_aninhada_da_erro_sintatico_amigavel`, `::test_cadeia_longa_de_operadores_sem_parenteses_nao_e_afetada`, `::test_cadeia_de_nao_muito_funda_da_erro_sintatico_amigavel`, `::test_cadeia_de_menos_unario_muito_funda_da_erro_sintatico_amigavel`, `::test_cadeia_de_potencia_muito_funda_da_erro_sintatico_amigavel`, `::test_blocos_aninhados_a_mais_da_erro_sintatico_nao_recursionerror` | **corrige suposição do plano (Etapa 2)** — bem coberto | a Etapa 2 confirmou (ao escrever testes que se revelaram duplicados) que os 4 pontos de recursão documentados em `parser.py:35-55` (parênteses, `nao`, `-` unário, `^`) e `LIMITE_PROFUNDIDADE_BLOCO` já tinham teste de disparo do limite; só faltava o lado oposto (cadeia moderada e legítima não dispara falso positivo) para `nao`/`-`/`^` — 3 testes novos adicionados (`test_cadeia_de_nao_moderada_continua_a_funcionar` e 2 análogos) |
| Erros sintáticos amigáveis (parênteses/vírgulas em falta) | `parser.py` | `test_correcoes_auditoria.py::test_parser_*` (5), `::test_virgula_a_mais_em_*` (3, um por cada `_parse_lista_virgulas` call site) | bem coberto, **exceto `ler()`** | `escrever()` e vírgula a mais (3 sítios) já tinham teste dedicado — só `ler()` sem argumentos era mesmo uma lacuna; fechada na Etapa 2 com `test_ler_sem_argumentos_e_erro_sintatico_nao_traceback_cru` |

## 3. Semântica — tipos/declarações/âmbito/constantes

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| `constante` (7 regras) | `semantics.py` | `test_novas_funcionalidades.py::test_constante_*` (7) | bem coberto | confirmado na Etapa 3: as 7 regras (ler, âmbito local, não reatribuir, não `ler()`, não `ref`, valor inicial obrigatório, não array) mapeiam 1:1 para os 7 testes; nenhuma lacuna |
| Sombreamento global/local/parâmetro | `semantics.py` (classe `Escopo`, linhas 37-64) | `test_regressao_base.py::test_parametro_sombreia_global`, `test_novas_funcionalidades.py::test_parametro_pode_sombrear_constante_global` | **corrige suposição do plano (Etapa 3)** — "3+ níveis" é estruturalmente impossível nesta linguagem, não uma lacuna de teste | `Escopo.raiz_funcao` só permite sombrear ao atravessar a fronteira função/global; dentro da MESMA função, `_nome_ativo` proíbe redeclarar um nome ativo em qualquer bloco `se`/`para`/`enquanto` aninhado (só permite REUTILIZAR depois do bloco anterior terminar, não sombrear em simultâneo) — e não há funções aninhadas na linguagem. Logo só existe 1 fronteira de sombreamento possível (parâmetro/local de função vs. global); "3+ níveis" pressupõe scoping por bloco, que este design deliberadamente não tem. Não escrito teste novo — escrever um exigiria simular um cenário que o compilador já rejeita por construção. |
| Matriz de compatibilidade de tipos | `semantics.py::_tipo_binop`/`_compativel`/`_tipos_comparaveis` | 26 testes novos em `test_correcoes_auditoria.py::test_matriz_de_compatibilidade_operador_tipo` (parametrizado) + `::test_sem_comparacao_de_igualdade_entre_booleano_e_outro_tipo` | **fechado nesta etapa** | o lado NEGATIVO (operador rejeita par de tipos errado) já estava bem coberto por ~50 testes `test_sem_*` existentes (confirmado por grep antes de escrever, lição da Etapa 2); o que faltava era o lado POSITIVO como tabela única — cada operador × par de tipos válido com o tipo de resultado inferido confirmado (ex.: `caracter + caracter` larga para `cadeia`, não fica `caracter`; `div`/`mod` nunca contaminam para `decimal`; `/` é sempre `decimal` mesmo com dois inteiros exatos) |

## 4. Semântica — estruturas / arrays N-d

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| `estrutura` aninhada/recursiva | `semantics.py` | `test_estruturas.py` (17, incl. lista ligada com `nulo`, recursão mútua) | bem coberto | |
| **`nulo` (palavra-chave)** | `lexer.py:13`, `semantics.py` | `test_estruturas.py::test_nulo_incompativel_com_tipo_primitivo_da_erro`, `::test_nulo_nao_pode_ser_usado_como_identificador`, `::test_lista_ligada_construida_e_percorrida_com_nulo`, `::test_aceder_a_campo_de_nulo_da_erro_amigavel_nao_traceback` | testado, **mas não documentado** | ver secção 6 (inconsistências documentais) — `nulo` não aparece em nenhum documento de `docs/`, nem `.md` nem `.docx` |
| `ref` com estrutura | `semantics.py` | `test_estruturas.py::test_ref_com_estrutura_altera_o_original` | coberto | |
| Literais `{...}` aninhados (struct-em-struct, struct-em-array) | `semantics.py` | `test_novas_funcionalidades.py::test_estrutura_literal_*` (4), `test_correcoes_auditoria.py::test_estruturas_*` (3), `::test_literal_de_estrutura_como_argumento_de_*` (3), `::test_devolver_literal_de_*` (4), `::test_array_de_literais_de_estrutura` | **corrige suposição do plano (Etapa 4)** — os 4 pontos de entrada de propagação de tipo (declaração, atribuição, argumento de chamada, `devolver`) já têm teste para ambos os tipos de literal onde sintaticamente possível; `B8` estava desatualizado (era verdade na 3ª auditoria, já não é) |
| Arrays N-dimensionais | `semantics.py`/`codegen.py` | `test_novas_funcionalidades.py::test_array_3d`, `test_array_literal_3d`, `test_correcoes_auditoria.py::test_array_4d_indexacao_e_atribuicao`, `::test_array_literal_4d` | **fechado na Etapa 4** — 4D confirmado (declaração+indexação e literal `{...}` aninhado); nenhum limite de dimensões no código (confirmado por leitura), extrapola-se para N arbitrário |
| Tamanho declarado vs literal | `semantics.py` | `test_correcoes_auditoria.py::test_array_com_tamanho_negativo_literal_da_erro`, `::test_array_com_literal_de_tamanho_diferente_do_declarado_da_erro`, `::test_array_com_literal_de_tamanho_igual_ao_declarado_compila` | **corrige suposição do plano (Etapa 4)** — "literal maior/menor que declarado" já tinha teste direto (`B7`/`AL-48`), não era lacuna |

## 5. Semântica — funções/`ref`/controlo de fluxo/`incluir`

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| `ref` com alargamento de tipo | `semantics.py` | `test_correcoes_auditoria.py::test_ref_decimal_nao_aceita_variavel_inteiro`, `::test_ref_cadeia_nao_aceita_variavel_caracter`, `::test_ref_com_tipo_exatamente_igual_continua_a_funcionar`, `::test_array_por_ref_com_tipo_de_elemento_errado_e_rejeitado` | **corrige suposição do plano** — bem coberto | plano previa isto como lacuna a confirmar; já coberto para decimal/inteiro, cadeia/caracter e array — reduzir prioridade na Etapa 5 |
| Mesma variável/campo por `ref` 2× na mesma chamada | `semantics.py` | `test_correcoes_auditoria.py::test_mesma_variavel_simples_passada_duas_vezes_por_referencia_da_erro` + 2 relacionados, `::test_campo_de_estrutura_dentro_de_array_por_referencia_duas_vezes_nao_e_detetado` (Etapa 5, novo) | coberto, incl. o limite conhecido | confirmado: campo de struct DENTRO de array (`pontos[0].x` 2×) nunca é detetado, por design (`_chave_ref_estatica` recusa comparar qualquer acesso com índice, mesmo com o mesmo literal nas duas chamadas) — não é um bug, é uma limitação documentada no código; teste novo fixa o comportamento atual (aliasing real acontece em runtime) como regressão, para não ser alterado por acidente |
| `incluir` recursivo/transitivo | `inclusoes.py` | `test_correcoes_auditoria.py::test_incluir_transitivo_resolve_biblioteca_que_inclui_outra` | coberto | |
| **Ciclo de `incluir` (A↔B)** | `inclusoes.py` | `test_correcoes_auditoria.py::test_incluir_transitivo_circular_nao_entra_em_ciclo_infinito` | **corrige suposição do plano** — já coberto | plano (Fase 2) assumia isto como lacuna sem teste — falso, já existe e cobre exatamente o cenário A→B→A |
| Colisões de `incluir` entre categorias | `inclusoes.py` (via `cli.py::_carregar_e_resolver_inclusoes`) | `test_correcoes_auditoria.py::test_incluir_estrutura_duplicada_da_erro` (+funcao/+variavel_global) e as 3 versões `_em_processo` novas da Etapa 5 | **confirmado na Etapa 5: é só ambiente, lógica correta** | as 4 falhas da baseline foram reproduzidas isoladas e o erro real é `FileNotFoundError: [WinError 2]` de `_winapi.CreateProcess` (executável `algo` fora do `PATH` desta sessão) — idêntico à causa das outras 78 falhas, não uma regressão nem bug na deteção de colisão. Escritas 3 versões em processo (`_carregar_e_resolver_inclusoes` direto, sem subprocess, mesmo padrão de `test_incluir_transitivo_*`) que confirmam a lógica de deteção está correta, imunes ao problema de ambiente |
| `escolher`/`caso` com tipos compatíveis, literais diferentes | `semantics.py` | `test_correcoes_auditoria.py::test_caso_duplicado_entre_cadeia_e_caracter_e_detetado`, `::test_caso_duplicado_entre_inteiro_e_decimal_e_detetado` (secção B11/AL-83, ~linha 3575) | **corrige suposição do plano/Etapa 0** — já coberto | achado da Etapa 5: grep mais específico por "já apareceu antes" (a mensagem de erro real) encontrou os 2 testes que o grep anterior (Etapa 0, por "1 vs 1.0 vs '1'" literal) não encontrou — cobre exatamente `1`/`1.0` e `"a"`/`'a'`; não escrito teste novo |

## 6. Geração de código / paridade `codegen` vs `codegen_minimo`

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Paridade comportamental sistemática | `codegen.py`, `codegen_minimo.py` | `test_paridade_codegen.py` (30, novo — corpus de 25 programas + 2 divergências documentadas + 3 regressões de bug) | **fechado na Etapa 6** | achado adicional: `test_compila_minimo.py` (36) está inteiramente na lista de falhas da baseline (depende de `algo` no `PATH`, tal como as falhas da Etapa 5) — inoperável nesta sessão; o novo ficheiro evita isso invocando o compilador em processo, só usando `sys.executable` (nunca depende do `PATH`) para correr o Python já gerado |
| **BUG real encontrado nesta etapa**: literal `{...}` aninhado (ou até um literal de estrutura simples como argumento) rebentava `codegen_minimo.py` com `ErroInternoCompilador` | `codegen_minimo.py`, `gerador_base.py::_gerar_atribuicao` | `test_paridade_codegen.py::test_literal_de_estrutura_como_argumento_de_chamada_em_minimo`, `::test_literal_de_estrutura_aninhado_em_atribuicao_em_minimo`, `::test_literal_de_estrutura_aninhado_em_devolver_em_minimo` + corpus (`estrutura_aninhada_literal`, `array_de_literais_de_estrutura`) | **corrigido nesta etapa** | causa: `codegen_minimo.py` nunca teve o par `_expr_estrutura_literal`/`_expr_array_literal` recursivo que `codegen.py` já tinha desde AL-78/B8 — os 4 pontos de entrada (declaração, atribuição, argumento, `devolver`) construíam campos/elementos chamando `_expr()` genérico, sem ramo para `A.EstruturaLiteral` aninhado. Um programa 100% válido (compila e corre bem em modo normal) fazia o `--minimo` falhar já em COMPILAÇÃO, não só em runtime — contradizendo o próprio contrato documentado de `--minimo`. Adicionados os 2 métodos + atualizados os 4 pontos de entrada; sem `_coagir_decimal` (mantém a ausência de coerção decimal já existente em `--minimo`, não introduz assimetria nova) |
| `matematica.potencia` vs `^` (base negativa/expoente fracionário) | ambos geradores | `test_correcoes_auditoria.py::test_potencia_com_expoente_*` (4) | coberto para os casos já conhecidos | procurar outros pares "gémeos" do mesmo padrão continua válido |

## 6b. Execução e erros em tempo de execução (Etapa 7)

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Tabela de exceções nativas traduzidas (`IndexError`/`_AlgoIndiceCadeiaInvalido`/`ZeroDivisionError`/`OverflowError`/`RecursionError`/`AttributeError`/`ValueError`) | `codegen.py` (handler em `_algo_programa`) | ampla, ver `AL-08/AL-09/AL-19/AL-21/AL-64/AL-65/AL-68/AL-85/AL-86/AL-91` | bem coberto por rondas anteriores | confirmado por leitura completa de `codegen.py`/`bibliotecas/*.py` — qualquer exceção fora desta lista propagaria como traceback cru; nenhuma encontrada nesta etapa |
| `RecursionError`, campo de `nulo` em runtime | `codegen.py` | `test_correcoes_auditoria.py::test_recursao_infinita_da_mensagem_amigavel_via_cli`, `test_estruturas.py::test_aceder_a_campo_de_nulo_da_erro_amigavel_nao_traceback` | **corrige suposição** — testes existem mas inoperáveis nesta sessão (mesma causa `subprocess.run(["algo", ...])` das Etapas 5/6) | versões `_em_processo` novas na Etapa 7 confirmam ambos os caminhos continuam corretos |
| `matematica.piso`/`teto` com valor não finito (infinito/NaN, via `conversao.paraDecimal`) | `bibliotecas/matematica.py` | `test_correcoes_auditoria.py::test_matematica_piso_de_infinito_da_overflow_amigavel`, `::test_matematica_teto_de_nan_da_erro_amigavel` (novos) | **fechado na Etapa 7** | célula da tabela função×input-adversarial nunca antes exercitada; confirmado traduzido corretamente, nenhum bug |
| `matematica.potencia` com expoente grande demais para caber num `float` | `bibliotecas/matematica.py` | `test_correcoes_auditoria.py::test_matematica_potencia_com_expoente_gigante_da_overflow_amigavel` (novo) | **fechado na Etapa 7** | idem — `OverflowError` na conversão final, já coberto pelo handler genérico, nunca testado especificamente via esta função |

## 7. Aplicação de consola (`cli.py`)

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Comandos + consola interativa | `cli.py` | `test_consola.py` (32, +1 na Etapa 8) | bem coberto por nome | **corrige a contagem da baseline**: na prática são 7 (não 17) que falham por `subprocess.run(["algo", ...])` sem `algo` no `PATH` (mesma causa das restantes 78) + 1 (`os.setsid`, POSIX-only, não existe no Python nativo do Windows — falha de portabilidade do PRÓPRIO TESTE, registada, não corrigida) — confirmado empiricamente na Etapa 8, um por um, não assumido |
| **BUG real encontrado nesta etapa**: `cli.py:main()` crasha com `UnicodeEncodeError` ao imprimir `✔`/`❌` quando invocado sem passar por `algo.bat`/`algo.sh` (ex.: `python -m algo_lang.cli`, ou o comando `algo` instalado corrido diretamente de um venv ativado à mão, sem o `chcp 65001`/`PYTHONIOENCODING` que só o `.bat` configura) | `cli.py::main` | `test_consola.py::test_cli_corre_via_python_dash_m` (já existia, estava a falhar; agora passa) | **corrigido nesta etapa** | AL-35 original só mitigou isto ao nível do script de arranque (`algo.bat` define `PYTHONIOENCODING`), não no próprio `cli.py` — qualquer invocação que bypasse o `.bat` (uma forma de uso já documentada pelo próprio teste) continuava vulnerável na code page por omissão do Windows (1252/850). Corrigido com `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` (+ `stderr`) logo no início de `main()`, protegendo todos os caminhos de invocação por igual |
| Nome de ficheiro com Unicode/acentuação | `cli.py` | `test_consola.py::test_consola_executa_ficheiro_com_nome_acentuado` (novo) | **fechado na Etapa 8** | lacuna prevista pelo plano; confirmado por execução direta antes de escrever o teste — já funcionava corretamente, nenhum bug |
| `--entradas` sem valor (`B24`/`AL-96`) | `cli.py::_linha_com_ficheiro_por_omissao` | `test_correcoes_auditoria.py` (linha ~3939) | já coberto, correção confirmada | teste puro em processo (sem subprocess), não depende do `PATH` — já funcionava antes desta etapa |
| `algo.sh`/`algo.command`/`algo.bat` | scripts de arranque | `test_algo_sh.py` (6, 3 marcados `slow` e por isso fora do âmbito de comparação com a baseline "not slow") | 2 de 3 não-`slow` falham na baseline (`OSError: [WinError 193]`, tentativa de correr um script bash como binário nativo do Windows) | confirmado: mesma limitação de plataforma conhecida (testar um script POSIX em Windows nativo sem invocação via shell), não um bug de lógica em `algo.sh` |

## 8. Ferramentas associadas

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Tracer (`--debug`/`--json`) | `tools/tracer.py` | `test_tracer.py` (20) | bem coberto por nome | 4 de 20 falham na baseline (mesma causa subprocess) |
| Flowchart | `tools/flowchart.py` | `test_fluxogramas.py` (16) | bem coberto por nome | 3 de 16 falham na baseline (mesma causa) |
| Linter | `tools/linter.py` | `test_linter.py` (50) | o mais bem coberto dos três | 3 de 50 falham na baseline |
| Consistência entre ferramentas | — | `test_consistencia_ferramentas.py` (novo, 2 testes) | **fechado na Etapa 9** | 1 teste "meta" corre o mesmo programa por compilador+linter+flowchart+tracer, confirmando que o conjunto de nomes de rotina que cada ferramenta deriva independentemente da AST é idêntico; 1 teste confirma que um campo de estrutura com o MESMO NOME de uma função real não ganha contorno duplo no fluxograma |
| Ambiguidade campo-de-estrutura vs chamada-de-biblioteca no linter/flowchart | `tools/linter.py`, `tools/flowchart.py` | ver acima | **corrige suposição do plano** — a ambiguidade é exclusiva da gramática TextMate do VS Code (texto puro, sem parser); linter e flowchart operam sobre a AST já tipada (`A.Chamada` vs `A.LValue` são nós distintos desde o parser), por isso não têm — nem podem ter — o mesmo problema. Confirmado por leitura de `flowchart.py::_eh_chamada_a_rotina` (começa por `isinstance(chamada, A.Chamada)`) e empiricamente pelo teste novo acima. Achado lateral, registado não corrigido: `Linter.codigo_fonte` é guardado em `__init__` mas nunca lido em lado nenhum da classe — parâmetro morto |

## 9. Extensão VS Code

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Paridade lexer↔gramática TextMate | `editors/vscode-algo/` | **corrige suposição do plano** — `test_correcoes_auditoria.py::test_vscode_grammar_nao_esquece_nenhuma_palavra_chave_do_lexer` já existia (secção B27/AL-99) | já satisfaz o pedido de "teste de paridade lexer↔gramática automatizado" do plano — confirmado na Etapa 10, não escrito de novo | a suite ad-hoc Node/JS referida pela 3ª auditoria confirmada AUSENTE deste repositório (só `package.json`/`language-configuration.json`/`syntaxes/`/`README.md` em `editors/vscode-algo/`, nenhum ficheiro de teste) — mas a lacuna real (nenhum teste automatizado) já tinha sido fechada por outro caminho |
| Campo de struct distinguível de chamada de biblioteca (requisito central da Etapa 10) | `editors/vscode-algo/syntaxes/algo.tmLanguage.json` (regra `library-calls`) | `test_correcoes_auditoria.py::test_vscode_grammar_library_calls_*` (3, novos) | **fechado na Etapa 10** | achado: só a LISTA de palavras-chave nos comentários da gramática tinha teste (parity acima) — o COMPORTAMENTO real dos padrões regex (`library-calls`, `declarations`) nunca tinha sido exercitado, só documentado em comentário. Confirmado com `re` do Python (sintaxe Oniguruma/TextMate coincide nas construções usadas: `\b`, lookahead positivo/negativo): `biblioteca.metodo(` reconhecido, `campo.acesso` (sem `(`) corretamente ignorado |
| Lookahead negativo de `declarations` (valor reservado em literal de estrutura, B29 da 3ª auditoria) | idem (regra `declarations`) | `test_correcoes_auditoria.py::test_vscode_grammar_declarations_*` (2, novos) | **fechado na Etapa 10** | mesma lacuna — só documentado em comentário, nunca testado; confirmado que `{ativo: verdadeiro}` não é mal interpretado como declaração `nome:tipo` |

## 9b. Testes profundos transversais — fuzzing, propriedades, mutation testing (Etapa 11)

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| Fuzzing por mutação do corpus válido + aleatório de baixo volume | `algo_lang/compilador/*` (parse/verificar/gerar_python/gerar_python_minimo/tokenizar) | `test_fuzzing_e_propriedades.py` (novo, 3 testes: 2000+ iterações de mutação de texto sobre os dois caminhos de codegen + 500 de texto aleatório sobre o lexer) | **fechado na Etapa 11** | 0 exceções não classificadas encontradas (seed fixo, reprodutível); exploração prévia mais ampla (8000+ iterações por caminho, incluindo `codegen_minimo.py`, a área onde a Etapa 6 encontrou um bug real) também não encontrou nada — fuzzing de mutação de TEXTO tem alcance limitado para bugs semânticos profundos como o da Etapa 6 (precisaria de um gerador estruturado/gramatical, fora do âmbito de "baixo volume") |
| Testes de propriedade: div/mod truncados | `codegen.py::_algo_div/_algo_mod` | `test_fuzzing_e_propriedades.py::test_propriedade_*` (2 propriedades × 8-14 casos parametrizados) | **fechado na Etapa 11** | identidade `a == (a div b)*b + (a mod b)` e sinal do resto igual ao do dividendo, confirmadas para positivos/negativos/zero |
| Testes de propriedade: idempotência de conversões | `bibliotecas/conversao.py` | `test_fuzzing_e_propriedades.py::test_propriedade_conversao_*` (3 propriedades) | **fechado na Etapa 11** | inteiro/decimal/booleano → texto → tipo original, confirmado idempotente |
| Mutation testing sobre `semantics.py`/`codegen.py` | compilador | `mutmut` (instalado nesta etapa; config em `pyproject.toml::[tool.mutmut]`) | **linha de base reportada, conforme pedido pelo plano** (não é um alvo a atingir) | **mutmut não corre nativamente em Windows** (bloqueio confirmado do próprio mutmut, exige WSL/Linux/macOS) — corrido via WSL (Ubuntu 24.04, já disponível nesta máquina), num venv scratch dedicado (`pytest`+`mutmut`, não persistido). **Resultado**: 2806 mutantes totais, **2239 mortos, 560 sobreviventes, 7 timeout/erro — score global 79.8%** (`semantics.py`: 1593 mutantes, 75.0%; `codegen.py`: 1213 mutantes, 86.1%). Sobreviventes concentram-se nos dispatchers maiores de cada ficheiro (`_verificar_stmt`/`_verificar_chamada` em `semantics.py`, `_expr`/`_gerar_stmt` em `codegen.py`) — esperado (mais ramos = mais pontos de mutação), não necessariamente bugs; não foi feita uma auditoria mutante-a-mutante dos 560 sobreviventes (fora do âmbito desta etapa, que pede só a linha de base). Config permanece no repositório para reprodução futura: a partir da raiz do repositório, `wsl`, depois `python3 -m venv .venv-mutmut && .venv-mutmut/bin/pip install pytest mutmut && .venv-mutmut/bin/python -m mutmut run` (gera uma pasta `mutants/` local, não commitada -- apagar depois de consultar `mutmut results`) |

## 10. IDs `AL-NN` — rastreabilidade correção↔código↔teste

83 IDs distintos encontrados no código (`AL-01` a `AL-99`, incluindo testes). 16 números da sequência 1-99 não aparecem em lado nenhum do código:

- **10 confirmados como "oportunidade de melhoria" deliberadamente não implementada**, documentados em `AUDIT_PLAN.md` secção 4 (Improvement Opportunities): `AL-10, AL-11, AL-14, AL-17, AL-20, AL-22, AL-25, AL-26, AL-27, AL-38`.
- **1 confirmado como decisão de design deliberada** (`AUDIT_PLAN.md` secção 8, Future Considerations): `AL-37` (leitura de stdin byte-a-byte no REPL).
- **1 (`AL-40`) documentado como "corrigido" em `AUDIT_DONE.md` mas sem tag correspondente no código** — só `AL-39` aparece (2×, em `codegen.py:387` e `codegen_minimo.py:167`, ambos sobre campos de tipo recursivo). Possível fusão de duas correções sob uma só tag no código, ou tag perdida. **Não é um bug funcional** — é uma lacuna de rastreabilidade documental, a registar, não a "corrigir" silenciosamente.
- **3 (`AL-03`, `AL-12`... na verdade `AL-12` existe em `AUDIT_DONE.md` como item UX-BAIXA de `cli.py`, texto de ajuda — confirmar se foi implementado sem tag) e `AL-30`, `AL-80` sem qualquer registo** em nenhum dos 4 documentos recuperados nem no código. Numeração possivelmente nunca atribuída, ou perdida em rondas anteriores de reorganização do documento de progresso.

**Achado desta etapa**: a rastreabilidade `AL-NN`↔código↔teste é, na prática, boa (>90% dos IDs esperados existem e têm teste), mas não é 100% auditável a partir do código sozinho — sem os 4 documentos recuperados do histórico git (que não estavam no repositório no início desta sessão), esta verificação teria sido impossível. Recomenda-se **não voltar a apagar os documentos de auditoria** no futuro (ou arquivá-los em vez de apagar) precisamente para permitir isto.

## 11. Inconsistências documentais encontradas

1. **`README.md`** diz "334 testes" em `algo_lang` — real (no início desta 4ª auditoria): 623 coletados. **Corrigido na Etapa 12**: `README.md:78` e `docs/ReferenciaCompletaCLI.md:662` atualizados para 792 (contagem final desta auditoria, ver secção 13).
2. **`nulo` (palavra-chave, tipo ponteiro-like para estruturas)** — real, testado (4 testes dedicados), historicamente causou um bug real na extensão VS Code (`B27`, "único das 33 palavras-chave sem highlighting nenhum") — mas **não aparece em nenhum documento de `docs/`**: nem `ManualCompletoALGO.md`, nem `ReferenciaCompletaCLI.md`, nem `ManualDaLinguagemALGO.docx` (0 ocorrências, confirmado por extração de texto). Um estudante que precise de estruturas recursivas (lista ligada, árvore) não tem onde aprender a sintaxe `nulo` a não ser pelos `exemplos/` ou por tentativa e erro.
3. **`ManualDaLinguagemALGO.docx`, `SebentaExerciciosALGO.docx`, `GuiaSolucoesALGO_Docente.docx`** não mencionam `fluxograma`, `linter`, `--minimo`, `--debug` nem `--json` (0 ocorrências em todos os 3, confirmado por extração de texto) — cobrem `estrutura`/`constante`/`afirmar` mas não a camada de ferramentas. Pode ser deliberado (documentos focados só na linguagem, não no CLI) — a confirmar com o responsável do projeto antes de tratar como lacuna a corrigir; registado aqui como observação, não como bug.
4. **`ErroSemantico` não tem parâmetro `coluna`**, ao contrário de `ErroLexico`/`ErroSintatico` que já o têm (e testado). Assimetria real no sistema de erros — não confirmado ainda se é intencional (semântica frequentemente não tem uma posição de coluna única e útil, ex. "tipo incompatível" pode abranger uma expressão inteira) ou uma lacuna. Fica para avaliação na Etapa 3.

## 12. Correções ao plano (Fase 2) resultantes desta etapa

Três "lacunas previstas" no plano (`AUDITORIA_PLANO.md`) revelaram-se **já cobertas** ao verificar contra os testes reais, e não devem consumir tempo de reprodução nas etapas seguintes:
- Etapa 2/plano: "segundo bloco `inicio`" — já coberto (`test_segundo_bloco_inicio_e_rejeitado`).
- Etapa 5/plano: "`ref` com alargamento de tipo, incluindo cadeia/caracter" — já coberto para todas as combinações verificadas.
- Etapa 5/plano: "ciclo de `incluir`" — já coberto (`test_incluir_transitivo_circular_nao_entra_em_ciclo_infinito`).

Uma "lacuna prevista" revelou-se **parcialmente errada**: "falta de coluna em erros" (citada da 3ª auditoria) — coluna já existe e está bem testada para léxico/sintático; o gap real e mais estreito é a ausência de `coluna` em `ErroSemantico`.

Um achado novo, não previsto no plano original: **4 testes de `incluir` com colisão de categoria** (`test_incluir_estrutura_duplicada_da_erro` e 3 relacionados) estão na lista de falhas da baseline — a confirmar na Etapa 5 se é falha de ambiente (subprocess) ou regressão real, já que a categoria "falha de ambiente" nunca tinha sido cruzada com estes testes especificamente.

## 13. Etapa 12 — Segunda passagem independente

Reauditoria das Etapas 6-9 delegada a um revisor sem acesso a este documento nem à `AUDITORIA_PROGRESSO.md` (para não herdar as conclusões próprias — ver relatório final, `algo_lang/AUDITORIA_RELATORIO_FINAL.md`, para a metodologia completa). Achado principal, confirmado por reprodução direta e corrigido:

| Requisito | Componente | Testes existentes | Estado | Nota |
|---|---|---|---|---|
| **BUG real encontrado nesta etapa**: `x:decimal = base ^ expoente` com `base`/`expoente` ambos `inteiro` e o expoente não é um literal — semantics.py tipa a expressão como `decimal` (não consegue provar em compilação que o expoente nunca é negativo), mas o `**` nativo do Python devolve `int` quando os dois operandos são `int` e o expoente calculado em runtime é não-negativo. Uma variável `decimal` ficava silenciosamente com um `int` (`y:decimal = 2^n` imprimia "8", não "8.0") — divergia também de `--minimo`, que já tratava este caso corretamente | `codegen.py::_expr` (ramo `A.BinOp` op `"^"`) | `test_paridade_codegen.py::potencia_inteira_com_expoente_variavel_atribuida_a_decimal`, `::potencia_encadeada_expoente_nao_literal` (novos); `test_correcoes_auditoria.py::test_cadeia_de_potencia_moderada_continua_a_funcionar` (existente, valor esperado corrigido de "16" para "16.0" — o valor antigo já era o mesmo bug, só nunca detetado porque `escrever()` direto não expõe o tipo declarado) | **corrigido nesta etapa** | Fix: `codegen.py` envolve a chamada a `_algo_pot(...)` em `float(...)` sempre que `expr._tipo_inferido == "decimal"` e os dois operandos são `"inteiro"` — situação em que a "decimalidade" vem só da incerteza estática do sinal do expoente, não de um operando realmente decimal (nesse caso o `**` nativo já devolve `float` sozinho). Ponto de geração único (`_expr`), por isso a correção cobre automaticamente todos os contextos (atribuição, argumento, elemento de array, campo de struct, `devolver`) |
| Restantes áreas das Etapas 6-9 (equivalência normal/`--minimo` para literais aninhados/`ref`/div-mod, mensagens de erro em runtime para todas as 7 categorias, `cli.py` — argumentos/encoding/exit codes, consistência tracer/flowchart/linter) | — | reexecutados com programas novos, escritos pelo revisor independente, em ambos os modos onde aplicável | **nenhum outro bug encontrado** | ver relatório final para o detalhe por subsistema e a classificação de confiança 1-5 |
| Testes vácuos (que passam sem validar nada) nos ficheiros de teste destas 4 áreas | `test_paridade_codegen.py`, `test_correcoes_auditoria.py`, `test_consola.py`, `test_consistencia_ferramentas.py`, `test_estruturas.py` | procura dirigida por padrões (asserts tautológicos, `except: pass` sem verificar tipo/mensagem, `pytest.raises` sem `match=` nem asserção adicional) | **nenhum encontrado** | amostras verificadas manualmente pelo revisor; ver relatório final |
| Documentação (`README.md`, `docs/ReferenciaCompletaCLI.md`) desatualizada face ao código atual | — | — | **2 desatualizações corrigidas**: contagem de testes (334→792, ver secção 11); lista de categorias de erro em runtime amigável (`ReferenciaCompletaCLI.md:547-551`) só mencionava 3 de 7 categorias reais — alargada | |

Suite completa sem regressão após a correção: **78 failed, 711 passed, 3 deselected** (792 coletados = 790 da Etapa 11 + 2 novos); 78 falhas idênticas por diff direto (mesma causa de ambiente de sempre).
